# Connectors — compliance

See the marketplace [CONNECTORS.md](../../CONNECTORS.md). Same included servers as `advice-core` (`.mcp.json`).

**Most useful:** `~~documents` (checklists, files), `~~chat` (watcher alerts), `~~calendar` (file-aging dates), `~~web monitoring` (regulatory-change watcher uses host web search when available).

Watchers degrade to workspace markdown when `~~chat` is not connected.
