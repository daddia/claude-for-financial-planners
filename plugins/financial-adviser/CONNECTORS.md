# Connectors — financial-adviser

See the marketplace [CONNECTORS.md](../../CONNECTORS.md). Same included servers as `advice-core` (`.mcp.json`).

**Most useful:** `~~documents` (templates, APL, PDS/TMD), `~~calendar` (review/FDS dates for `fds-renewal-watcher`).

**Not in V1:** XPLAN/Iress Open Standard API MCP. Use pasted CRM/holdings extracts.
