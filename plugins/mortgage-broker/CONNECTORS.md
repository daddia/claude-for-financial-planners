# Connectors — mortgage-broker

See the marketplace [CONNECTORS.md](../../CONNECTORS.md). Same included servers as `advice-core` (`.mcp.json`).

**Most useful:** `~~documents` (policy PDFs, trail export), `~~calendar` (expiries for the trail-book watcher), `~~e-signature` (category only — not pre-wired).

**Not in V1:** Connective/Mercury, AFG, ApplyOnline, Quickli, Sherlok MCP. Paste calculator outputs and policy PDFs.
