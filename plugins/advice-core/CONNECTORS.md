# Connectors — advice-core

See the marketplace [CONNECTORS.md](../../CONNECTORS.md) for placeholders and AU platform notes.

**Included in `.mcp.json`:** Slack (`~~chat`), Gmail (`~~email`), Google Drive (`~~documents`), Google Calendar (`~~calendar`), Notion (`~~knowledge base`), Microsoft Graph Enterprise.

**V1:** no first-party `~~crm` MCP (XPLAN, AdviserLogic, aggregator CRMs). Paste extracts. Skills work without any connector.
